# 260803 Update

## Changes Summary

- Changed dev locale directory from `src/locales` to `src/messages`.

## Major File Changes

- `src/templates/src/i18n/load-messages.ts`: Update `loadDevelopmentMessages` path to `src/messages`.

---

## Changes Summary

- Applied prettier formatting to not-found page.

## Major File Changes

- `src/templates/src/app/[locale]/not-found.tsx`: Reorder imports and className tokens.

---

## Changes Summary

- Cleaned up error page: removed unused `reset` prop, fixed useEffect deps, formatted.

## Major File Changes

- `src/templates/src/app/[locale]/error.tsx`: Remove `reset` prop; add `t`/`common` to useEffect deps; simplify onClick; format className.

---

## Changes Summary

- Deleted unused `CookieUpdater` component.

## Major File Changes

- `src/templates/src/components/cookie-updater.tsx`: Removed file.

---

## Changes Summary

- Refactored layout: moved `getMessages` to server import, removed `CookieUpdater`, added `lang` attribute.

## Major File Changes

- `src/templates/src/app/[locale]/layout.tsx`: Import `getMessages` from `next-intl/server`; remove `CookieUpdater` component; set `lang={locale}` on `<html>`.

## Feature Changes

- [Changed] `getMessages` import moved to `next-intl/server`.
- [Removed] `CookieUpdater` component (locale cookie no longer set).
- [Improved] Added `lang` attribute to `<html>` for accessibility.
