# 260725 Update

## Changes Summary

- Removed deprecated `baseUrl` option from tsconfig.json to fix TypeScript 6.0 typecheck error.

## Major File Changes

- `tsconfig.json`: Removed `baseUrl` option deprecated in TypeScript 6.0.
- `src/utils/copy-templates.ts`: Added utility to copy template files to dist on build.
- `.gitignore`: Updated ignore patterns (lock, build, deployment files).
- `src/templates/`: Added Next.js template project scaffolding.
  - Config: package.json, tsconfig, next.config, postcss, eslint, .gitignore.
  - App shell: pages, layouts, wireframes, styles, public assets.
- `tsconfig.json`: Excluded `src/templates` from root typecheck to prevent JSX/React type errors.
- `eslint.config.js`: Excluded `src/templates` from root lint to prevent eslint-config-next resolution errors.
- `.gitignore`: Added `dist/` to ignore build output directory.
