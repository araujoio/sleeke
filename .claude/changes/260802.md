# 260802 Update

## Changes Summary

- Simplified globals.css: removed shadcn theme variables, kept only tailwind import and base layer.

## Major File Changes

- `src/templates/src/styles/globals.css`: Strip all theme variables, keep only `@import "tailwindcss"` and base body styling.

---

## Changes Summary

- Applied prettier formatting to `loadMessages` utility.

## Major File Changes

- `src/templates/src/i18n/load-messages.ts`: Format function signatures per prettier.

---

## Changes Summary

- Applied prettier formatting to `readJson` utility.

## Major File Changes

- `src/templates/src/utils/readJson.ts`: Format function signature.

---

## Changes Summary

- Hardcoded default locales in the template's routing config.

## Major File Changes

- `src/templates/src/i18n/routing.ts`: Use inline `locales: ["en"]` and `defaultLocale: "en"` instead of reading `../../sleeke.json`.

## Feature Changes

- [Changed] Default routing to English locale.

---

## Changes Summary

- Enabled the next-intl plugin in the template's Next config.

## Major File Changes

- `src/templates/next.config.ts`: Wrap config with `next-intl/plugin`, pointing at `src/i18n/request.ts`.

## Feature Changes

- [Added] next-intl integration via Next.js plugin in the template.

---

## Changes Summary

- Extracted shared `readJson` helper used by the template's locale loader.

## Major File Changes

- `src/templates/src/i18n/load-messages.ts`: Use `@/utils/readJson`; type via `AbstractIntlMessages`; hardcode `src/locales` dev path; drop `../../sleeke.json` config import.
- `src/templates/src/utils/readJson.ts`: New shared JSON reader helper.

## Feature Changes

- [Added] `readJson` utility shared by locale loading.
- [Refactored] `loadMessages` to use the shared helper and `AbstractIntlMessages`.