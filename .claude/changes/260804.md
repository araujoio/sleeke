# 260804 Update

## Changes Summary

- Added home page content and metadata from translations.

## Major File Changes

- `src/templates/src/app/[locale]/page.tsx`: Import `useTranslations` and `getTranslations`; add `generateMetadata`; render `home.title`.

---

## Changes Summary

- Refactored not-found page translation keys: switched from `common` to `meta` namespace, renamed `not-found.action` to `not-found.button-action`.

## Major File Changes

- `src/templates/src/app/[locale]/not-found.tsx`: Use `meta` namespace for site title; rename translation key.

---

## Changes Summary

- Added dynamic metadata generation from translations.

## Major File Changes

- `src/templates/src/app/[locale]/layout.tsx`: Import `getTranslations` and `Metadata`; add `generateMetadata` using `meta` namespace.

---

## Changes Summary

- Refactored error page translation keys: switched from `common` to `meta` namespace, renamed `internal.*` to `server-error.*`.

## Major File Changes

- `src/templates/src/app/[locale]/error.tsx`: Use `meta` namespace for site name; rename translation keys to `server-error.*`.
