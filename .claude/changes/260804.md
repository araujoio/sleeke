# 260804 Update

## Changes Summary

- Added sleeke as devDependency to template package.json.

## Major File Changes

- `src/templates/package.json`: Add `sleeke: ^0.1.0` to devDependencies.

---

## Changes Summary

- Simplified message fallback: removed unused error parameter and Portuguese error message.

## Major File Changes

- `src/templates/src/i18n/request.ts`: Remove `error` param from `getMessageFallback`; remove conditional error throw.

---

## Changes Summary

- Simplified metadata generation: removed namespace, use top-level keys.

## Major File Changes

- `src/templates/src/app/[locale]/layout.tsx`: Remove `namespace: "meta"` from `getTranslations`; use `title`/`description` keys directly.

---

## Changes Summary

- Fixed typo: renamed `erros.json` to `errors.json`.

## Major File Changes

- `src/templates/src/messages/en/erros.json`: Deleted.
- `src/templates/src/messages/en/errors.json`: Renamed file.

---

## Changes Summary

- Added English locale message files for the template.

## Major File Changes

- `src/templates/src/messages/en/erros.json`: Error page translations (not-found, server-error).
- `src/templates/src/messages/en/meta.json`: Site metadata translations (title, description).
- `src/templates/src/messages/en/views/home.json`: Home page translations.

## Feature Changes

- [Added] English locale messages for template pages.

---

## Changes Summary

- Added home page content and metadata from translations.

## Major File Changes

- `src/templates/src/app/[locale]/page.tsx`: Import `useTranslations` and `getTranslations`; add `generateMetadata`; render `home.title`.

---

## Changes Summary

- Refactored not-found page translation keys: switched from `common` to `meta` namespace, renamed `not-found.action` to `not-found.button-action`.

## Major File Changes

- `src/templates/src/app/[locale]/not-found.tsx`: Use `meta` namespace for site title; rename translation key.

---

## Changes Summary

- Added dynamic metadata generation from translations.

## Major File Changes

- `src/templates/src/app/[locale]/layout.tsx`: Import `getTranslations` and `Metadata`; add `generateMetadata` using `meta` namespace.

---

## Changes Summary

- Refactored error page translation keys: switched from `common` to `meta` namespace, renamed `internal.*` to `server-error.*`.

## Major File Changes

- `src/templates/src/app/[locale]/error.tsx`: Use `meta` namespace for site name; rename translation keys to `server-error.*`.
